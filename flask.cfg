[buildout]
parts =
    flask

find-links +=
    file://${buildout:directory}/pypi-local

[flask]
recipe = zc.recipe.egg
eggs =
    flask
    flask_jwt_extended
    flask-mqtt
    flask-restful
    flask-socketio
    gevent
    gunicorn
    requests
